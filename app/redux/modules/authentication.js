const initialState = {
  isAuthed: false,
  isAuthenticating: false,
  autheId: ''
}

export default function authentication (state = intitialState, action) {
  switch (action.type) {
    default :
    return state
  }
}
